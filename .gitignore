# --- secrets: never commit ---
.env
.env.*
!.env.example
!.env.local.example
*.pem
*.key
.dev.vars
.dev.vars.*
!.dev.vars.example

# --- cloudflare / wrangler ---
.wrangler/
.open-next/
worker-configuration.d.ts

# --- node ---
node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo
.turbo/

# --- test / coverage ---
# Anchored on purpose: a bare `coverage/` also matched web/src/app/coverage/,
# the Coverage page itself, which is how it went uncommitted.
/api/coverage/
/web/coverage/
playwright-report/
test-results/
.playwright/

# --- local db ---
*.sqlite
*.sqlite3
*.db

# --- os / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# --- merge / patch leftovers ---
*.orig
*.rej

# --- logs ---
*.log
npm-debug.log*

# Cloudflare token policy dumps — account metadata, not project files
JSON encoding
*.token.json
